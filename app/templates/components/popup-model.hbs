
{{!-- {{log this.model "model"}} --}}
<div class="popup-model bt-radious bb-radious">
    <div class="popup-container">
        <div class="header popup-header bt-radious">
            <span > Select {{this.type}} </span> 
            <span class="cls-button" onclick={{action "close"}}>&#215;</span>
        </div>
        <div class="search-bar">
            <i class="fa-solid fa-magnifying-glass search-icon"></i>
            <select class="select dropdown-selector">
                <option value="Groups">Group</option>
                <option value="Devices">Devices</option>
            </select>
            <input class="input-box" type="text" placeholder="Search by Device Name and Group Name">
        </div>
        <div class="popup-model-content">
            {{#if this.component_type}}
            {{#each this.groups as |group|}}
            {{#if group.selected}}
            <div class="group-container selected" onclick={{action "selectToggle" group}}>
                <i class="fa-sharp fa-solid fa-folder-closed icon"></i>
                <div class="group-component">
                    <span class="group-name">{{group.name}}</span>
                    <span><i class="fa-solid fa-mobile"></i> {{group.data.length}} devices</span>
                </div>

            </div>
            {{else}}
            <div class="group-container " onclick={{action "selectToggle" group}}>
                <i class="fa-sharp fa-solid fa-folder-closed icon"></i>
                <div class="group-component">
                    <span class="group-name">{{group.name}}</span>
                    <span><i class="fa-solid fa-mobile"></i> {{group.data.length}} devices</span>
                </div>

            </div>
             {{/if}}
                
            {{/each}}
            {{else}}
            {{#each this.groups as |group|}}
            {{#if group.selected}}
            <div class="group-container selected" onclick={{action "selectToggle" group}}>
                <i class="fa-brands fa-{{group.os}} icon"></i>
                <div class="group-component">
                    <span>{{group.device}}</span>
                    <span><i class="fa-solid fa-mobile"></i> {{group.username}} devices</span>
                </div>

            </div>
            {{else}}
            <div class="group-container" onclick={{action "selectToggle" group}}>
                <i class="fa-brands fa-{{group.os}} icon"></i>
                <div class="group-component">
                    <span>{{group.device}}</span>
                    <span><i class="fa-solid fa-mobile"></i> {{group.username}} devices</span>
                </div>

            </div>
            {{/if}}


            {{/each}}
            {{/if}}

        </div>
        <div class="popup-model-footer bb-radious">
            <input type="button" class="footer-select-btn bb-radious bt-radious" value="Select {{this.selectedItems.length}}">
        </div>
        
    </div>
</div>

{{yield}}