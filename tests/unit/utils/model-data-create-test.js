import modelDataCreate from 'device-group-manager/utils/model-data-create';
import { module, test } from 'qunit';

module('Unit | Utility | model-data-create', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = modelDataCreate();
    assert.ok(result);
  });
});
